---
title: "Mosoo API"
description: "在你的应用中通过 API 集成并调用发布的 Mosoo Agent。"
---

# 基于Mosoo Agent 集成开发

你的应用提供 API token 和已发布 Agent 的 `agentId`；Mosoo 会创建 Thread、启动 Agent，并向 API 调用方开放这个 Thread 的公开事件和文件。

这就是 Agent 发布菜单里 **API 接入** 对应的集成能力。

<Note>
  API 只适用于已发布的 Agent。Agent 配置、模型服务商连接和渠道绑定都需要先在 Mosoo 中完成。
</Note>

<Columns cols={3}>
  <Card title="发布 Agent" icon="1">
    在 Mosoo 中连接模型服务商，配置并**发布 Agent **以获得 `agentId`。
  </Card>

  <Card title="创建 API token" icon="2">
    在用户头像- settings - API tokens 创建标识你身份的 `API token`
  </Card>

  <Card title="创建 Thread" icon="3">
    携带 `API token `\+`agentId` 通过API 创建 Thread，发送事件、文件并读取结果。
  </Card>
</Columns>

## 第一个请求

把 `API token `和 `AgentID`组合起来，就可以创建 Thread。

```bash
curl -X POST "https://mosoo.ai/api/v1/agents/$MOSOO_AGENT_ID/threads" \
  -H "Authorization: Bearer $MOSOO_API_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: ticket-182-create-thread" \
  -d '{
    "client_external_ref": "ticket-182",
    "input": {
      "type": "user.message",
      "content": [
        {
          "type": "text",
          "text": "Triage this customer escalation and suggest next steps."
        }
      ]
    }
  }'
```

响应里会包含 `thread.id` 和第一个 `run`。后续发送消息、读取事件、附加文件和管理生命周期都使用这个 Thread ID。

<Card title="查看完整快速开始" icon="rocket" horizontal href="/zh-Hans/quickstart">
  设置环境变量，创建 Thread，发送下一条用户消息，并读取事件日志。
</Card>

## 核心概念

<Columns cols={2}>
  <Card title="Agent" icon="user-astronaut">
    已经发布的Agent，可以通过 API 调用在 Agents 面板和Agent 详情页获取它的`agentId`。
  </Card>

  <Card title="API token" icon="key">
    是调用 Mosoo API 的身份凭证，用来证明调用者是谁，并决定其对 Agent 和 Thread 的可见性。
  </Card>

  <Card title="Thread" icon="microblog">
    API 创建的对话容器。Thread 记录历史消息、文件、运行状态、和 Agent 的生命周期状态。
  </Card>

  <Card title="Event" icon="unity">
    Thread 中的输入、输出、工具、文件、运行状态和用量这些公开可见变化，都是一种 Event。
  </Card>
</Columns>

## 可以用来做什么

当你希望把 Agent 接入自己的产品或工作流时：

- 当工单、订单、告警或任务出现时，从你的后端启动 Agent。
- 在运行前或运行过程中给 Thread 附加文件。
- 轮询公开事件日志，读取 Agent 消息、工具状态、文件变化、用量更新和运行完成状态。
- 使用 `client_external_ref` 和 `Idempotency-Key` 让外部系统与 Mosoo 保持一致。

## API 能力范围

<Columns cols={2}>
  <Card title="创建和列出 Threads" icon="list-tree" href="/api-reference/create-a-thread-for-a-published-agent">
    `POST /agents/{agentId}/threads` 创建 Thread 和初始运行。`GET /agents/{agentId}/threads` 列出调用方可见的 Threads。
  </Card>

  <Card title="发送和读取事件" icon="send" href="/api-reference/send-user-messages-permission-decisions-or-interrupts-to-a-thread">
    `POST /threads/{threadId}/events` 发送用户消息、权限决策或中断。`GET /threads/{threadId}/events` 读取公开事件日志。
  </Card>

  <Card title="文件管理" icon="paperclip" href="/api-reference/add-a-thread-file">
    添加、列出和移除 Thread 文件。API 添加的文件是附件；Agent 生成的文件会作为产物文件返回。
  </Card>

  <Card title="管理 Thread 生命周期" icon="archive" href="/api-reference/archive-a-thread">
    当集成不再需要 Thread 保持活跃时，可以读取、归档、恢复或删除 Thread。
  </Card>
</Columns>

## 访问模型

每个请求都会同时校验已发布 Agent 和 API token：

- Agent 必须存在、已发布，并启用 API 接入。
- API token 必须有效且未被撤销。
- 调用方仍需具备访问此 Agent 的权限。
- ~~运行时会使用已发布 Agent 所有者配置的能力边界。~~

<Card title="理解 token 和访问控制" icon="shield-check" horizontal href="/zh-Hans/auth-and-access">
  了解 API token、Agent 访问路径、归因和所有者配置如何配合。
</Card>